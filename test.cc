// Unit test file

#include "watcard.h"

#include <iostream>
using namespace std;

_Task WatCardTester {
public:
private:
};

void testWatCard() {
    WATCard testCard;
    assert(testCard.getBalance() == 0);
}

void uMain::main() {
    uProcessor p[16] __attribute__((unused));

    testWatCard();
}
